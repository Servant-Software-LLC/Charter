# Guardrail review — 221-review-log-unknown-state

Plan-Definition-Hash: sha256:716b335dc4d4c644225b0165e9567b4140a20d5fc0a9b748e813dc607094b8b0

Reviewed 2026-09-05. `validate` OK · `graph --check` was **exit 2 (stale) after the fixes and was
regenerated** to match the folder — task 07 gained two guardrails during this pass. `samples verify`:
2 pairs, 0 findings.

**Self-review disclosure.** Every guardrail in this folder was authored by the same agent in the
immediately preceding turn and committed in `a377dd4`. No human had edited any of it, so nothing here
is a reviewer proposing changes to someone else's work — and the pass carries the blind spots of a
self-review. Findings 1–3 were found by *executing* things; none was found by re-reading.

## Verdict

Six findings, all **addressed**. No BLOCKER left open.

| # | Task | Guardrail | Severity | What slipped through | Fix applied |
|---|---|---|---|---|---|
| 1 | 01·03·05·07 + terminal `01-solution-builds` | `01-tests-build.ps1` | **BLOCKER** | A DLL **file lock** was reported as *"your test file has a syntax or reference error"* — and the retry agent's `writeScope` **is** that file, so every attempt rewrites correct code while the lock persists, dead-ending at `needsHuman` with a confident wrong diagnosis | branch on `: error CS` vs `MSB3026/7`; the lock path now says ENVIRONMENT, NOT YOUR CODE and directs a `needsHuman` escalation |
| 2 | 07 | *(absent)* | **BLOCKER** | `[Trait("Feature",…)]` written once on the **partial class** instead of per method. A C# attribute on any partial declaration applies to the whole type, so `Category=BrowserAcceptance&Feature=ReviewLogUnknownPanel` silently widens to **every browser test**, turning task 08's `tests-pass` into a project-wide gate (#455) — while every check stays green | added `03-every-test-carries-the-feature-trait.ps1` (ported from the 234 plan, retargeted) + a two-sided sample pair |
| 3 | `preflights/01-baseline-suite-green`, `guardrails/02-all-tests-pass` (+5 more) | re-emit block | WEAK | xUnit prints its whole `[FAIL]` **name list** before any detail, so a `[FAIL]`-anchored 55-line window starves the detail — the exact #179 failure the re-emit exists to prevent | anchor on the first `Error Message:`, fall back to `[FAIL]` only when no detail block exists (a crashed host) |
| 4 | 07 | *(absent)* | WEAK | `WaitForFunctionAsync` throws under the served page's CSP, which makes 07's red census pass for the **wrong reason** (a CSP throw, not a vanished panel) and surfaces at 08 one task too late (#221) | added `04-no-forbidden-playwright-idioms.ps1` + a two-sided sample pair whose valid half uses `WaitForEventCountAsync` |
| 5 | 07 | prompt | WEAK | prompt said `[Fact(Skip=…)]`; this project spells its browser tests `[SkippableFact]` | prompt now names the real attribute and the per-method trait requirement |
| 6 | — | Step 7.4 report | NIT | the bolded `Seam ledger (#382)` heading carried a *dependency*-seam table, not the six-column substituted-fake ledger with bucket cells | corrected below |

### Corrected seam ledger (#382)

_No in-process seam is substituted by this breakdown's tests._

Re-derived from the folder (check 7): task 07's Playwright route intercept is a **process/HTTP** seam,
which the doctrine explicitly excludes from the ledger — and it lets the **real server** answer, so
nothing is substituted at all, only the timing is the test's. Task 01's transient-failure row exercises
the **filesystem**, likewise a process seam. No `IPromptRunner`-shaped in-process collaborator is faked
anywhere in this plan.

## Evidence — what was executed

- **#248, verified by running the tool.** The summary regex reads `Failed: 0, Passed: 5` → **5**; a
  skipped-only summary → **0** (the `Total:` trap avoided); a zero-match run emits **no summary line at
  all** and the guard fires (`$ran -lt 1` = True) — constructed, not read.
- **#302, 13 two-sided runs** across all four censuses: good / one-green / one-skipped / one-missing /
  zero-executed / longer-sibling / case-only. All behaved. The last two confirm `(\(|$)` and `-cmatch`
  are load-bearing.
- **Finding 1, measured live**: `dotnet build` on `Charter.Cli.Tests` → **0 `CS####` errors, 44
  `MSB3026` warnings**, exit 1. Re-tested against the *same still-locked tree* after the fix: the
  guardrail now names the environment.
- **Finding 3, measured**: real committed failing log (8 failures) → 6 `Error Message:` blocks survive;
  synthesised 60-failure regression → **56 names, 0 blocks**. After the fix, same input → **7 blocks**.
  Fallback re-tested on a names-only log: anchor still found.
- **Probe B operator 21** (hollow test) **dies** against the censuses — a `Passed`-on-stub-tree row
  exits 1.
- **Probe A** on the two new guardrails: both RED on the baseline via their precondition.
- **#500 positive control**: the four-`Category` collision sweep returned 0 hits, and the *same
  invocation* returned 1 for `BrowserAcceptance` — proving the search opened the files.
- **§4 missing-insertion pointed at the GATE folders** (`preflights/`, `guardrails/`), separately from
  the `tasks/*/` sweep: all seven files the union gate reads are tracked on master. Clean.
- **#578**: 23 structural claims in the action prompts were executed one command at a time during
  breakdown; two were corrected (`BrowserEngine.cs` is not a partial of `ReviewLoopBrowserTests`;
  `charter.css` lives under `src/Charter.Core/assets/`) and one doc citation was re-anchored to the
  code fact it actually rests on.

## Unchecked gaps

- **Probe B operator 20 — NOT RUN, inapplicable.** Greenfield first review: the real-seam test does not
  exist yet, so the mutation has nothing to mutate. Not folded into "Probe B applied".
- **Probe A ran 5 of 15 toolchain-invoking guardrails** against the real tree (3 task guardrails + the
  2 new ones). The remainder share one generator with the executed ones and were covered by the 13
  synthetic-TRX runs; they were not individually executed against a live `dotnet test`.
- **The A₂ census tree is the PLAN's baseline, not each task's.** A non-root task's clause could be
  pre-satisfied by an ancestor's output, and that tree does not exist at review time. The textual
  substitute was run: the four `Category` values and the `Feature` value appear zero times in `tests/`
  today, with a positive control.
- **`01-tests-build.ps1` is GREEN on a clean baseline by design** — a compile gate, not an adequacy
  floor. Declared in its own header; the red proof is guardrail 02. Legitimate under #155's
  `build-passes` + `tests-fail-on-stubs` pair rather than under A₂'s enumerated exception list.
- **No prompt-judges exist in this plan**, so the model-availability probe has nothing statically named
  to resolve (`promptRunners.claude.model` is null). No JIT-resolved judge to defer to #223.
- The `guardrails/03-review-log-union-verified.ps1` union gate is the plan's only source-shape check and
  ships **without** a sample pair — the `samples/` mechanism is task-scoped and this gate is plan-level.
  Its two-sided behaviour was reasoned about, not executed.
