# Guardrail review (round 3) — 234-diagram-expand-mode.charter

Plan-Definition-Hash: sha256:d74891acbac130644052d4f4b22c83fed90146273e40093190c26c08ecec3e88

Supersedes round 2 (`review-cd1cc19e5b98-…`). **The plan has already been executed to green and merged**
(Charter PR #250, master `65db0378`); this round records one hardening to a committed guardrail and
re-attests the changed definition. No new findings against the plan's substance.

## The change

`tasks/01-author-tests-expand-affordance/guardrails/03-every-test-carries-the-feature-trait.ps1` now
resolves its subject as **`GR_SUBJECT` → `$args[0]` → the hardcoded target**, where round 2 honoured only
`$args[0]`.

Both are supplied by the same `SampleVerifier` call, so round 2's fix worked and the pair verified
green — but **`GR_SUBJECT` is the canonical half of the contract**, and Guardrails **#559** (open) proposes
a `guardrails-review` probe asserting by name that every sampled guardrail honours it. An `$args[0]`-only
guardrail passes the verifier today and fails that probe when it lands. Accepting both costs three lines
and cannot be broken by whichever half a future verifier drops.

## Verification — all four resolutions executed

| mode | subject | expected | got |
|---|---|---|---|
| `GR_SUBJECT` set | `…valid.cs` | exit 0 | **exit 0** |
| `GR_SUBJECT` set | `…invalid.cs` | exit 1 | **exit 1**, naming the untraited method |
| no env, no args | the real test file | scans the real target | **exit 0** — the file exists on master since the merge |
| `guardrails samples verify` | the harness's own verifier | 0 findings | **`OK: 1 sample pair(s) verified, 0 findings`** |

Note the third row changed meaning since round 2. There it exited 1 on a precondition because
`DiagramExpandAffordanceTests.cs` did not exist yet; the plan has since run and created it, so normal mode
now genuinely scans the real file and passes. That is the guardrail working, not a regression.

Solution build clean (0 warnings, 0 errors); `Charter.Cli.Tests` 326/326 green.

## Rounds 1–2, carried forward

All findings applied and unchanged: the union invariant's `charter-expand` gate is a stated contract in
task 02's prompt (and the shipped implementation honours it — 6 references in `sdk/charter-annotate.js`);
six `tests-pass` re-emits use a bounded block capture rather than a line allowlist; the census messages are
worded for both worktree and serial mode; and round 2's own finding — the sample pair that could not fail —
is the reason this file exists at all.

The standing caveat also carries forward: **this plan was reviewed by the agent that authored it.** Round 2
exists because an independent executor (the harness) caught what the self-review had declared verified, and
this round exists because an independent report (#559) named a contract the self-review had settled by
probe. Both corrections came from outside the pass.
