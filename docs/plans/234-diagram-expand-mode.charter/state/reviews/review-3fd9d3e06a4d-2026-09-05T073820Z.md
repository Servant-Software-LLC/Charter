# Guardrail review — 234-diagram-expand-mode.charter

Plan-Definition-Hash: sha256:3fd9d3e06a4dd0df4ca95af6ee8cac929791633c64ebccdc9b6c4672a6de7714

Reviewed 2026-09-05 UTC. **Verdict: no BLOCKERs. Three findings, all applied.**

> **Reviewer independence — read this first.** This pass was run by the same agent that authored the
> breakdown. The adversarial pass and the authoring pass therefore share a mental model, which is the
> exact condition that produces a confident, clean read over a folder with holes in it. Both findings
> of substance below came from **executing** something, not from re-reading; the reading half of this
> pass should be treated as weaker than the executed half.

## Findings

| # | Task / gate | Guardrail | Severity | Wrong implementation that slipped through | Fix applied |
|---|---|---|---|---|---|
| 1 | terminal gate | `guardrails/03-sdk-union-verified.ps1` | WEAK | The contribution-present check gates on `$content -match 'charter-expand'`, but **no action prompt required that class name** — the sole mention was inside task 02's *prohibition* about `charter.css`. An agent naming the class `charter-fullscreen` leaves the gate false forever, so the tightening never fires. Union soundness was still covered by the conflict-marker half (which is what credits GR2028), so this was dead weight rather than a false green. | Task 02's prompt now **requires** the `charter-expand` stem and states why it is a contract rather than a style preference. |
| 2 | 6 guardrails | all `tests-pass`-family re-emits | WEAK | Allowlist-shaped failure re-emit (#608). Measured against a synthetic `Assert.DoesNotContain` failure, the allowlist re-emitted 3 lines and **dropped** `String:`, `Found:` and both stack frames — the retry agent would see that a test failed but not what the actual value was. | Replaced with a **block capture** from the first failure marker onward, bounded to 55 lines. Re-measured on the same input: 9 lines, payload and stack frames intact. |
| 3 | tasks 01, 03 | census failure messages | NIT | "No expand control exists yet" is accurate in worktree mode (03 branches from 01 only) but misleading under `maxParallelism: 1`, where 02 merges before 03 runs. | Reworded to "the expanded view cannot be entered / until the invariants are implemented". |

## Probe B operator 21 — the result worth keeping

Manufactured the hollow mutant for task 01: seven test methods, **correct names, correct `Feature`
traits, `Assert.True(true)` bodies**, and ran the task's own guardrails against it.

```
03-every-test-carries-the-feature-trait  ->  EXIT 0   "7 test method(s), all carrying the trait"
02-tests-fail-on-current-code (census)   ->  EXIT 1   all 7 behaviours named individually
```

The naming floor **certified seven tautologies as covered**. The per-test red census caught every one.
This is #375 demonstrated on this folder rather than argued, and it is the reason the census must not
later be "simplified away" in favour of the cheaper floor. Mutant deleted; tree verified clean.

## Guards proven to FIRE, not read

- **Census zero-match guard** — run against the real tree: the TRX carries no `<Results>` element and
  the guard exits 1. The `| Where-Object { $_ }` filter is load-bearing; the bare `@(...)` form would
  evaluate `1 -lt 1` and never fire.
- **Forward zero-match guard** — constructed the zero case (`--filter FullyQualifiedName~ZzNoSuchTestClass`):
  `dotnet test` exits **0**, and the executed-count expression computes `$ran = 0` → fires. The guard is
  the only thing between a typo'd filter and a green no-op.
- **All 13 scripts parse** (`Parser::ParseFile`, 0 failures) — a parse error is a #473 dead-end no retry
  can fix, and the block capture added by finding 2 is new code in a failure path.

## #578 — structural claims executed, with a positive control

Every claim the prompts make about the codebase was settled by command, not by re-reading:
`WaitForSelectorWhileTouchingAsync` (4), `WaitForEventCountAsync` (12), `SaveComposerAsync` (11),
`TryLaunchAsync` (87), `showScrollbars` (9), `.table-scroll` (45), `disableChrome` (5),
`restoreChromeFocus` (9), `clearMarkers` (17), the `#51` section marker (1),
`document.body.appendChild(root)` at line **2576**, `make`/`button` helpers (2), 86 browser tests.
**Positive control:** `pinDiagramChrome` = 8 in `sdk/charter-annotate.js`, confirming the searches
reached the file rather than silently skipping it (#500). All verified; none corrected.

## What this pass could NOT check

- **Reviewer independence** — see the note at the top. The single largest gap.
- **Probe A skipped 8 toolchain-invoking guardrails** (both `tests-build`, both `tests-pass`, both
  artifact-invariance, `01-solution-builds`, `02-all-tests-pass`) — minutes each.
  `03-both-engines-pass` is genuinely not author-time-runnable: WebKit is not installed.
- **Probe A₂ is largely vacuous on this plan.** Every required-present clause's subject
  (`DiagramExpandAffordanceTests.cs`, `DiagramExpandInvariantTests.cs`, `charter-expand` in the SDK) is
  **absent from the baseline**, so those clauses never execute and the census has nothing to measure.
  Recorded as NOT RUN with that reason — not as clean. The censused tree is the plan's baseline, not
  each task's; no ancestor-output tree exists at review time.
- **Operator 20: INAPPLICABLE** — greenfield first review, no real-seam test exists yet. Not run; not
  passed.
- **Operator 21 ran against task 01 only.** Task 03's census is the identical script with different
  method names; it was not separately mutated.
- **§4 missing-insertion was pointed at the gate folders**, not only `tasks/*/`: the terminal gate's
  producer set is all four tasks, and `charter-expand` in the SDK is produced by tasks 02 and 04. No
  `<plan>/preflights/` exists (the #181 baseline was deliberately skipped — see below).
- **#474 datum clauses:** exactly one, the union guardrail's `charter-expand`. Carrier is the SDK file,
  owned by tasks 02 and 04 — reachable. Finding 1 is its other half: reachable, but not *required*.
- **Probe C:** no require × forbid collisions. The only forbidden patterns are the line-anchored
  conflict markers, which no required literal trips.
- **Seam ledger:** zero-row form, re-derived from the folder — no prompt directs faking an in-process
  seam (Playwright drives the real page). Correct as a claim, checked rather than assumed.
- **Tiering:** not configured (no `routing` block, no `tiering` block), so the #229 probe produced
  nothing and this report says nothing further about it.
- **#181 baseline preflight deliberately absent.** The touched area is the browser suite, which boots a
  review server and a browser — it fails the worth-it gate's "deterministic and cheap, no live-service
  boot" test. Under-fired on purpose; a flaky baseline halts a correct plan before the DAG.

## Post-fix verification

`guardrails validate` → **OK: plan is valid**. `guardrails graph --check` → **exit 0** (fresh).
Census re-run after the reword → still fires. Working tree clean apart from the plan folder itself.
