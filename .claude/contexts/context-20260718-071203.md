REPO: C:\DevAI\Charter
TOPIC: (none)

CONTEXT:
Session goal was a status assessment of the Charter repo (no prior Claude
history existed *in this repo*), then landing the outstanding work. Charter is
the front door to an agentic delivery pipeline (Charter -> Guardrails ->
firstmate): an AI authors a block-structured markdown plan; a human reviews it
in-browser and comments in place; the reviewed markdown feeds Guardrails, which
breaks it into a verified task DAG. C#/.NET, single self-contained binary.
Combines Lavish's comment-in-place review loop with Builder.io visual-plan's
block authoring. Plan of record: docs/plans/01-combine-lavish-and-visual-plan.md.

WHERE THE PROJECT HISTORY LIVES (important):
- This repo (C--DevAI-Charter) had NO prior Claude sessions and an EMPTY memory
  dir before this session. The real Charter design/genesis conversation lives in
  the lavish-axi project folder, because the work was done while cwd'd there.
- Main history session: 9f0023aa-e59d-4202-b292-acc3ef6786ce (7.2 MB, ~2129
  events, cwd C:\DevAI\Samples\lavish-axi, spans Jul 10 -> Jul 18). Resume with:
  claude --resume 9f0023aa-e59d-4202-b292-acc3ef6786ce  (from C:\DevAI\Samples\lavish-axi)
- The other lavish-axi session 51c0a535 (Jul 10, 8 min) is unrelated (folder-rename troubleshooting).
- One Charter-related memory exists, saved under the lavish-axi project (NOT here):
  C:\Users\David\.claude\projects\C--DevAI-Samples-lavish-axi\memory\charter-guardrails-firstmate-autonomous.md
  -> Charter + Guardrails must support a complete autonomous/unattended mode for
  firstmate crews, persisting deliverables to disk for hindsight review.
  Tracked as Charter #7 + Guardrails #361.

COMPLETED (this session):
- Assessed repo status end-to-end (git, Guardrails run state, GH issues, history).
- (a) Fast-forwarded master to cfb05d4, landing WAVE-1 renderer core that had
  been stranded in a temp Guardrails worktree/branch
  (guardrails/01-combine-lavish-and-visual-plan). Files: src/Charter.Core/
  {BlockModel,CharterRenderer,SourceMap}.cs, src/Charter.Cli/Program.cs (render
  verb), Charter.Core.csproj (+Markdig 0.37.0), and 3 test files
  (RendererGoldenTests, SourceMapTests, StableIdTests).
- Verified merged master: `dotnet test Charter.sln -c Debug` => 12/12 pass, clean build.
- (b) Committed the WAVE-2 review-server plan-breakdown JIT draft (f41f1d5):
  docs/plans/01-combine-lavish-and-visual-plan/wave-02-review-server/** (4 tasks:
  scaffold -> author tests -> implement -> wire review CLI, + preflight + wave
  exit gate), the regenerated wave-aware diagram.html/diagram.md/guardrails.baseline,
  and a new plan-folder .gitignore for transient Guardrails run state (issue #258).
  Transient state/run.json + state/state.json + logs/ correctly excluded.
- Pushed master to origin: 5ccc247..f41f1d5. origin/master == f41f1d5.

WAVE-1 GUARDRAILS RUN FACTS (runId 2026-07-17T12-43-37Z-35db, run 1d4e39a5):
- All 3 wave-1 tasks succeeded first attempt; wave-1 exit gate passed. ~$10.37 total.
- Renderer design: stable id = 'b' + hex(first 10 bytes of SHA-256(normalized
  block source-span)); content-derived so ids survive edits to unrelated blocks.
  SourceMap.LineForAnchor => 1-based start line. AutoIdentifiers intentionally OFF.
  Single shared CharterMarkdown parse helper guarantees render id == block-model id.

IN PROGRESS / NEXT STEPS:
- WAVE-2 (review server) is a DRAFT only: committed but NOT yet reviewed or run.
  Next: /guardrails-review docs/plans/01-combine-lavish-and-visual-plan/wave-02-review-server
  -> guardrails mark-reviewed -> guardrails run (JIT resume against real wave-1 code).
- WAVES 3-6 (annotation+feedback loop, rich/interactive blocks incl. :::question,
  export + Guardrails handoff, agent skill + polish) are still just milestones in
  the plan doc; not broken down yet.
- Consider issue #7: autonomous/headless mode for firstmate (net-new vs original v1 scope).
- Optional cleanup: the Guardrails worktrees/branches for run 1d4e39a5 still exist
  under %TEMP%\guardrails-worktrees\... and as local branches
  (guardrails/01-combine-lavish-and-visual-plan, guardrails/1d4e39a5/...). Safe to
  prune once satisfied wave-1 is on master (commits are in the repo object store
  regardless of the temp dirs).

KEY FILES (relative to repo root):
- docs/plans/01-combine-lavish-and-visual-plan.md  (plan of record)
- docs/plans/01-combine-lavish-and-visual-plan/{diagram.md,diagram.html,guardrails.json,guardrails.baseline}
- docs/plans/01-combine-lavish-and-visual-plan/wave-02-review-server/**  (wave-2 draft)
- docs/plans/01-combine-lavish-and-visual-plan/state/guardrails-review.json (tracked; run.json/state.json are gitignored)
- src/Charter.Core/{BlockModel,CharterRenderer,SourceMap}.cs ; src/Charter.Cli/Program.cs
- tests/Charter.Core.Tests/{RendererGoldenTests,SourceMapTests,StableIdTests,PlanDocumentTests}.cs

CROSS-REPO:
- lavish-axi (C:\DevAI\Samples\lavish-axi): holds the genuine Charter genesis
  conversation (session 9f0023aa) and the Charter firstmate-autonomous memory.
- Guardrails: Charter's downstream consumer. Related open Guardrails issues: #361
  (autonomous mode, cross-linked to Charter #7), #359/#360 (human-gate / JIT wave
  checkpoint), #258 (transient-state gitignore). The plan was broken down and run
  with the Guardrails CLI (waved plan + JIT stub-wave resume).

BLOCKERS/NOTES:
- guardrails.baseline (committed) still references logs/... hashes even though
  logs/ is now gitignored -- committed faithfully as the tooling left it, not
  hand-edited (would corrupt Guardrails' integrity manifest). A future
  `guardrails run`/`validate` on wave-2 will reconcile it. Not a blocker.
- GH issues: 7 open, 0 closed. #1-#6 are deliberate v1 deferrals/decisions; #7
  (Jul 17) is the firstmate autonomous-mode ask.
- User's env enforces: never `cd <path> && git ...`; always `git -C <root> ...`
  (PreToolUse hook blocks the former).
