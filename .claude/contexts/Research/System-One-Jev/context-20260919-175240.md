REPO: C:\DevAI\charter
TOPIC: Research/System-One-Jev

CONTEXT: What we were working on

David asked for research into whether TypeSafe's **System One** model class — its first model is
**Jev** — could take over narrow judgment calls that Charter (and, it turned out, Guardrails too)
currently get an LLM to make by reinforcing them in skill prose. The session read Jev's docs
end-to-end, mapped candidate decision points in Charter's own skills and code, filed a research
issue, then — after David pointed out that an open-weights System One model could remove the
"stays offline" objection for in-binary candidates — added that as a tracked revisit condition.
David then revealed he had *already* filed a matching research issue on the Guardrails side
(#755, pre-existing, not created this session), so the session's second half was tying the two
together under one epic and cross-linking everything, including a now-closed Guardrails issue
(#361, the autonomy dial) whose `CriticalityJudge` confidence is #755's first candidate.

No files in this repo's tracked source were changed. All substantive output this session is
**GitHub issues and comments**, across two repos.

COMPLETED:

- Read TypeSafe's docs (`docs.typesafe.ai`) via WebFetch: System One concept page, primitives
  (Choice/Score/Noul), models page (`jev-1.13.0`, pricing, limits, retention), the jev-1.13
  "jaggedness" known-weaknesses page, the skill-suggestion cookbook (closest prior art: routing an
  agent's skill roster), and the agent-skill page (Claude Code plugin).
- Surveyed Charter's own skills (`skills/charter/SKILL.md`, `skills/charter-drain/SKILL.md`,
  `skills/charter/references/{authoring-plans,unattended,authoring-from-source}.md`) and code
  (`src/Charter.Core/DeferralLint.cs`, `src/Charter.Core/MarkdownConvert.cs`) for existing
  LLM-or-regex judgment calls that fit Jev's Choice/Score/Noul shape.
- Filed **Charter#264** — "Research: can a System One decision model (TypeSafe's Jev) take over
  narrow calls Charter's skills currently train the agent to make?" (`question` label). Contains:
  a Jev facts table with citations, a "where faster can come from" framing (Charter's CLI is
  already fast; gains only come from replacing agent turns or shrinking skill text), an 8-row
  candidate table, constraints (binary stays offline per #6, plan content is sensitive, no new
  fuzzy paths into correctness, determinism stays load-bearing), and research questions
  (latency, accuracy-on-our-data, turn/token savings, opt-in shape, per-candidate verdict).
- Posted a follow-up comment on Charter#264 addressing David's in-process/open-weights point:
  which objections go away (data egress, API keys/outages, latency) vs. what still holds
  (advisory-only, cross-OS determinism risk — float results can differ x64 vs arm64 near a
  threshold, causing a golden-file flake on the 3-OS CI matrix — and the model version becoming
  part of the contract like `charter-format-version`), new costs (package size vs. NuGet's 250MB
  cap given 5 self-contained RIDs, cold start on short-lived `charter render` processes), and a
  concrete suggestion: prototype the plumbing *now* with an existing open-weights zero-shot
  classifier, independent of waiting for a real System One OSS release.
- Learned **Guardrails#755 already exists** (David's own issue, not created this session) —
  the Guardrails-side counterpart, covering criticality assessment, unknown-failure widening,
  overwatcher diagnosis, run-boundary fault triage, prompt-judge guardrails, and difficulty
  routing. It already had David's own open-weights-model comment on it.
- Verified **Guardrails#361** (the autonomy dial) is **CLOSED/COMPLETED**, delivered via PR #394
  — corrected course to link #755 as the live counterpart rather than treating #361 as open.
- Created **Guardrails#756** — "Epic: System One typed-decision models (Jev) — replacing
  free-text LLM judgment calls with calibrated typed ones, across Guardrails and Charter"
  (`enhancement` label). Attached Guardrails#755 and Charter#264 as **native GitHub sub-issues**
  (via `gh api -X POST .../sub_issues`, cross-repo attachment worked). Body sets shared ground
  rules: advisory-never-certifying, one provider-agnostic interface, data-egress is the
  maintainer's per-repo call, measured-not-quoted verdicts, Charter-reviewed designs — plus a
  shared "when to revisit" (open-weights, permissive license, runs locally/in-process).
- Posted a comment on **Guardrails#361** pointing to #755/Charter#264/#756: the dial's
  `escalationThreshold` currently compares against `CriticalityJudge`'s self-reported confidence,
  which is exactly the uncalibrated number a System One model would replace. Says nothing
  reopens; any change flows from a #755 "go" verdict as its own issue.
- Posted a comment on **Guardrails#755** naming Charter#264 as the counterpart, the shared
  provider-interface need, Charter's strongest candidates (deferral lint, issue-deferral match)
  and their measurement plan, the #361 confidence thread, and the in-process/open-weights
  argument mirrored from Charter's side.
- Edited **Charter#264**'s body: replaced the placeholder "may be worth a mirror note" sentence
  about Guardrails with concrete links to #755 and the #756 epic.
- Registered a new context-registry topic `Research/System-One-Jev` in
  `.claude/contexts/_registry.md` (only local file change this session).

IN PROGRESS: (none — both issues and the epic are in a stable, cross-linked state; next actions
are follow-on research work, not loose ends of this session)

NEXT STEPS:

- **Decide on the OUTBOX message.** I offered to write a message to the Guardrails session in
  its `docs/asks/OUTBOX.md` (per [[guardrails-prompt-not-a-summary]] convention — second person,
  paste-able, monotonic `seq`) telling it the epic/cross-links now exist. David had not answered
  before this `/context-save` ran. If he says yes: write to `C:\DevAI\Guardrails\docs\asks\OUTBOX.md`
  (Guardrails' own repo, not Charter's), following the stamp format in that memory file, commit
  and push it there, then just tell David "a new prompt is waiting for you."
- **Charter#264 research work** (whoever picks this up, likely the charter-developer /
  charter-architect agents): build small labelled sets from real plans/issue history for the two
  strongest binary-adjacent candidates (`DeferralLint.cs` regex, and "is the linked issue really
  about the deferred item"), then measure Jev against the incumbent on accuracy, calibration,
  latency, and no-answer rate — not vendor claims.
- **Guardrails#755 research work**: per its own plan, audit `git log` skill-reinforcement diffs
  into buckets (a) typed-decision-fixable / (b) generative-drift / (c) harness-bug-in-prose; then
  replay the criticality judge (its best first candidate, using #533's evidence corpus of
  GREEN/RED-labelled outcomes) through the current judge, Jev, and a locally-calibrated
  constrained-decoding baseline.
- **Provider-interface prototype**: whichever repo starts first should build the
  "questions in → `{option→probability}` + confidence out" interface once, behind one
  abstraction, so the other repo reuses its shape rather than inventing a second one. This can
  start now with an open-weights zero-shot classifier as a stand-in for Jev/OSS-System-One, per
  the addendum comment on Charter#264.
- **Any "go" verdict** becomes its own issue, filed as a sub-issue of Guardrails#756, and any
  resulting design goes through a Charter review (`.charter.md` under `docs/plans/`) — never a
  draft PR, per [[user's global CLAUDE.md design-review rule]].

KEY FILES:

- `skills/charter/SKILL.md` — chart-the-work vs chart-the-data disambiguation table (candidate 1)
- `skills/charter-drain/SKILL.md` — orphaned-annotation guidance, revise-vs-reply (candidates 5, 6)
- `skills/charter/references/unattended.md` — "What raises needsHuman — exactly four things"
  (ground truth for what must stay deterministic, never delegated to a model)
- `skills/charter/references/authoring-from-source.md` — block-type choice (candidate 8)
- `src/Charter.Core/DeferralLint.cs` — regex deferral lint, explicitly says "the LLM lives in the
  agent, never in the binary" (candidate 2; the standing principle any in-binary use must revisit)
- `src/Charter.Core/MarkdownConvert.cs` — open-questions heading/list-item heuristic (candidate 3)
- `.claude/skills/charter-dev-knowledge/SKILL.md` (~line 490) — packaging/distribution facts:
  NuGet dotnet tool, single-file binaries for 5 RIDs, Homebrew tap, used to reason about weight
  package size and the 250MB NuGet cap
- `.claude/contexts/_registry.md` — added the `Research/System-One-Jev` topic entry this session
- `docs/plans/02-architecture-b-living-document.md:212`, `docs/plans/03-git-mediated-team-review.md`
  §4.3 (~line 273) — "exact-hash-or-orphan" anchor decision (#50), cited as the reason candidate 5
  (orphan-annotation hints) must stay advisory-only, never an anchor fallback

CROSS-REPO:

- **Servant-Software-LLC/Charter#264** (this repo) — research issue, `question` label, sub-issue
  of Guardrails#756.
- **Servant-Software-LLC/Guardrails#755** — pre-existing (David-authored) research issue on the
  harness side; now a sub-issue of Guardrails#756, with cross-link comments added this session.
- **Servant-Software-LLC/Guardrails#756** — new epic issue, `enhancement` label, created this
  session; sub-issues Guardrails#755 + Charter#264 attached via GitHub's native sub-issues API.
- **Servant-Software-LLC/Guardrails#361** — closed/delivered autonomy-dial issue; received a
  pointer comment this session, not reopened.
- Referenced but not touched: Guardrails#533 (replay bench / evidence corpus), Guardrails#570
  (local-inference roadmap, Mac Studio end-of-Oct-2026), Guardrails#201 (model-tiering epic),
  Charter#6 (zero-telemetry decision), Charter#7 (autonomous mode, deliberately thin/no dial),
  Charter#50 / plan 03 §4.3 (exact-hash-or-orphan anchors).
- The Guardrails-side OUTBOX file this offer would write to is
  `C:\DevAI\Guardrails\docs\asks\OUTBOX.md` (a **different repo/working tree** from this one —
  per [[guardrails-prompt-not-a-summary]], write only in your own repo, never the counterpart's).

BLOCKERS/NOTES:

- No blockers. The one open decision is purely David's: whether/when to send the OUTBOX ping to
  the Guardrails session, since he hadn't replied to that offer before running `/context-save`.
- Key facts about Jev worth not re-fetching: hosted only (`POST /v1/systemone`, model
  `jev-1.13.0`, aliases `jev-latest`/`jev-preview`), no .NET SDK (Python/JS only), $0.042/M input
  tokens and free output, 64k tokens/request (32k for state+question), 1200 req/min & 250k
  tokens/s (adjusting dynamically), zero-retention is enterprise-only, no published latency
  figure. Known weaknesses (jev-1.13 "jaggedness"): literal reading, unreliable counting, weak on
  hex/RGB and date/time-as-text comparison, accuracy falls with irrelevant state in context,
  vulnerable to adversarial content, no cross-question consistency guarantee, not built for
  generation. Prior-art cookbook (skill-suggestion, 182 skills / Haiku 4.5 / 488 requests): wrong
  skill loads 16.8%→7.3%, needless loads 9.8%→4.0%.
- Ground rules both issues/epic now share (don't relitigate on a resume): advisory-only (a model
  answer never decides an anchor, applies an answer, clears `needsHuman`, or sets an exit code),
  one provider-agnostic interface across repos, data-egress is a per-repo maintainer call (moot
  once local/in-process), verdicts must be measured on our own data not vendor claims, and any
  resulting design is Charter-reviewed, never a draft PR.
