REPO: C:\DevAI\Charter
TOPIC: Dogfooding/Charter-Guardrails

CONTEXT: What we were working on

Continuation of the dogfooding thread. The prior save (context-20260821-213331.md) ends with
Charter's backlog at ZERO and a Stage 2 handoff written for the Guardrails session. Since then a
single reported bug (#164) cascaded into THIRTY closed issues, fourteen merged PRs, the v0.24.0
release, and a machine-consumer contract built out from nothing.

Master now sits 18 commits AHEAD of the released v0.24.0 tag. Work is deliberately PAUSED waiting
on four questions the Guardrails lane owes an answer to (see NEXT STEPS).

LANES (unchanged, still load-bearing)

Drawn by the repo that RECEIVES THE COMMIT, not by who found the bug. Dogfooding crosses repos, so
whoever drives a run files the issue in whichever repo it belongs to — but the FIX goes to that
repo's session, because that is where the build, tests and release live.

- This session = Charter lane (C:\DevAI\Charter). Held cleanly throughout.
- A separate session owns Guardrails (C:\DevAI\Guardrails). Seen live and busy 2026-08-23.

STAGE 2 IS GUARDRAILS, NOT CHARTER. Stage 2 = Guardrails #226 (resolve a task's difficulty tier to
a concrete provider/model/effort at attempt-launch). Charter is the TOOL used to author and review
its plan; every file it changes is in the Guardrails repo.

COMPLETED

--- Earlier sessions (v0.19.0 → v0.23.0), carried forward ---

#137 note selection jumps + marks its anchor, persists, arrow-key navigation · #142 warn on an open
human select-question with no `recommended` (`null` is the deliberate opt-out) · #143 the `charter`
skill gates on loading `charter-format` first · #147 SessionRegistry prunes on read and write; pid
is a NEGATIVE signal only, plus a start-time check defeating pid reuse · #144 the page hands over
`/charter-drain <plan>` (a skill), not a command line · #116 RE-SCOPED, spawning dropped; the page
offers `charter skills install` beside the invocation · #152 skills carry their version in the BODY
· #157 agent replies attributed to the agent ("Agent (via <name>)") · #158 reviewers can REPLY in a
thread and it reaches the agent in the same round · #156 untracked-deferral lint · #154 `skills
install --force` refuses to clobber git-tracked source · #155 charter-format states its version
range in its BODY, bound to code by a test.

--- This session: v0.24.0 (released) ---

The reviewer-facing release. All eight found by USING the tool, none by testing it.

- #164 annotation count badges on lists/tables/rules. The deny-list was correct about the HTML
  content model but its stated fallback was weaker than it looked: under `forced-colors` the accent
  bar computes to `none`, so an annotated list was PIXEL-IDENTICAL to an unannotated one. Filed as
  cosmetic; it was an accessibility defect wearing a cosmetic one's clothes.
- Lists got per-<li> ANCHORS rather than a badge — the machinery already shipped for `:::comparison`
  and only needed its gate widened. A note on a twelve-item list used to hand the agent "the list".
- #165 #167 #168 #169 #170 the marker reachable and its absence explained · #166 the opaque-region
  containment predicate · #171 a link reference definition no longer steals the line-1 anchor (it
  also DUPLICATED the plan title in the CommonMark Guardrails consumes) · #176 #177 #178 #179 the
  SDK owns what it built · #180 closed, already fixed by #166 · #184 #190 blocks that are not direct
  children · #191 a Status block states only bound facts · #194 (yours) skills install into a git
  work tree says so · #198 the badge probe measures what the page shows · #200 a rebuild hands focus
  back.

--- This session: the machine-consumer contract (UNRELEASED, 18 commits on master) ---

- #172 #173 `charter handoff --fail-if-needs-human`, the in-band `plan-sha256` stamp, `.headless.json`
  declared a contract, schema 1 → 2.
- #186 #188 an answers file may FILL a decision, never REPLACE one. "answered" had FIVE
  implementations; all now route through one kernel.
- #187 the handoff manifest — one resolution pass, one record of what it decided, plus the second
  in-band stamp line `answers-sha256`.
- #175 the flatten carries its link reference definitions · #192 `charter verify`.
- #202 a control character is refused where it ENTERS, not cleaned where it is emitted.
- #203 a nested `:::question` rendered an answerable form invisible to the block model — and the
  answer was DRAINED, REPORTED APPLIED, AND DESTROYED. Reproduced verbatim before fixing.
- #207 an example fence wide enough to hold its contents, plus a guard for the class · #208 a nested
  `:::diff` renders instead of crashing · #209 #204 a stale queue read no longer erases a newer note.

IN PROGRESS

Nothing mid-edit. Working tree clean, all worktrees removed, merged branches pruned, everything
pushed. Master = f336538.

NEXT STEPS (Charter lane — PAUSED BY CHOICE)

Deliberately holding. Do NOT cut a release until the four questions below are answered, because
Charter's side of all four is unreleased and therefore still cheap to change.

Three issues remain open, none release-blocking:
- #212 a control character HAND-AUTHORED into a question's title/options/recommended. Format-wide
  (its natural home is `QuestionSpec.TryParse`, which would move `charter-format`), so it was
  deferred rather than decided unilaterally.
- #216 readiness gates timed out at 15s on healthy runs — FIXED in #218, filed so the reasoning is
  reviewable.
- #217 under load, `poll`'s session probe returns exit 3 (NoSession) where 4 (DrainError) is
  correct. NOT fixed. The real question is whether that timeout can misreport a LIVE server as
  absent in production, which would mislead a pipeline in the exact direction #172 exists to prevent.

Also unclosed from the #209 sweep, reported not changed: `ready` is the ONLY once-per-page event —
`review-log-loaded`, `list-loaded`, `round-loaded` and `panel-opened` all fire at `init()` too, so
~30 "first occurrence" waits are satisfied by page load. And one test waits on `review-log-changed`
where the agent's reply is never actually awaited, so its stated claim is UNPROVEN.

NEXT STEPS (Guardrails lane — FOUR QUESTIONS OWED)

Full paste-able text: guardrails-ask-20260823.md beside this file. Both reciprocal issues are still
OPEN and unanswered as of 2026-08-24.

1. Is the delegated-question marker workable? (blocks Guardrails #500). Charter's docs ASSERTED the
   headless breakdown branches on `target: human|agent`. It does not — the literals Charter emits
   appear ZERO times across Guardrails' src, docs and skills. Charter corrected its own false claim
   and now emits `**Delegated decision — you must settle this before building:**` plus a
   mode-specific `_Decide: …_` line. Only Guardrails knows if that shape works.
2. Which provenance surface will #496 assert on? Four now exist: the in-band `plan-sha256`, the
   in-band `answers-sha256`, `.headless.json` (schema 2, UNRELEASED), and `<out-stem>.manifest.json`.
   A field they assert on becomes FROZEN — the failure #173 was filed to prevent, which already
   happened once when `recommended` was added with no schema bump.
3. Is a fourth artifact acceptable — and will they fix step 2? Their harness asserts `charter
   headless` exits 0, which is a STRICTLY WEAKER predicate than the gate (the gate also blocks an
   undecidable agent question and an unknown `:::foo`). A run strict handoff would refuse passes
   their step 2 today. This is also why Charter will NOT add `--answers` to `headless` though it is
   the smaller change: it would make step 2 look correct while still testing the wrong thing.
4. Will `guardrails` record the SHA-256 of the `plan.md` it consumed? (Guardrails #505). Verified:
   their `PlanHash` hashes `guardrails.json` plus every `task.json` — NOT the markdown they were
   broken down from. Until something does, Charter's `handoffSha256` is a tamper detector with no
   consumer, and #496's "did the plan Charter recorded match the plan Guardrails consumed" stops one
   hop short of what it names.

KEY FILES

- .claude/contexts/Dogfooding/Charter-Guardrails/guardrails-ask-20260823.md — the four questions
- .claude/contexts/Dogfooding/Charter-Guardrails/stage-2-handoff-20260821.md — the Stage 2 handoff
- docs/plans/04-machine-consumer-contract.md — Rev 1→7. THE design of record for #172/#173/#186/
  #187/#188/#175/#192/#202/#203. Read in full before touching the handoff path
- src/Charter.Core/HandoffManifest.cs · HandoffAnswers.cs · HandoffGate.cs · AnswerRules.cs
- src/Charter.Cli/VerifyCommand.cs — `charter verify`, and the disclaimer it prints ON SUCCESS
- src/Charter.Core/NestedDirectiveLint.cs — #203. Predicate is "does this render LIVE", never
  "is this nested"
- src/Charter.Core/DirectiveFence.cs — #190, the ONE fence vocabulary
- sdk/charter-annotate.js — the ownership ledger (#176), the focus ledger (#200), `disableChrome`
  (#204), the write clock (#209)
- tests/Charter.Cli.Tests/SkillFenceBalanceTests.cs — #207's class guard
- .claude/agents/charter-ux.md — NEW this session; see BLOCKERS

CROSS-REPO

- Guardrails #500 (delegated-question routing) and #505 (record the consumed plan's hash) both filed
  by this session, both OPEN.
- Guardrails state verified 2026-08-21: v1.8.0; #451/#457/#452/#455/#459 CLOSED; #458/#460/#456
  OPEN. STALE — re-verify before relying on it.
- Charter #155's sibling is Guardrails #466 (was open) — neither tool can fix that pairing alone.

BLOCKERS/NOTES

- charter-ux was ADDED as a team agent this session and earned its place immediately. Charter is a
  CLI whose product surface is a page a human reviews in, and every reviewer-facing issue was found
  by a person using it, never by a test. It overturned the architect's stated blocking concern by
  MEASURING (the "occludes every table header" cost was 0px at every width) and found the
  forced-colors defect that reclassified #164. Its operating contract's load-bearing line: WALK THE
  REAL PAGE and say which document you opened.
- The SCHEMA-2 WINDOW CLOSES AT THE NEXT RELEASE. Three changes deliberately rode the unreleased
  `HeadlessRecord.Schema = 2` rather than forcing a 3. That is only cheap while no consumer has seen
  it. Verify with `git show v0.24.0:src/Charter.Core/HeadlessRecord.cs` before assuming.
- RESTART the session after `charter skills install --force`. The loaded copy does not change when
  the file on disk does — exactly what #152 exists to make visible.
- NuGet's tool-resolution index lags its flat-container index. After a release, `dotnet tool update`
  can fail even with `--version` while the package IS on the feed. `dotnet nuget locals http-cache
  --clear` plus one retry fixes it; the flat container had it at 240s on the 0.24.0 cut.
- CI: a conflicting PR runs NO CI at all — GitHub cannot build a merge ref, so the PR sits at zero
  checks, which reads exactly like "not started yet". `mergeable=CONFLICTING` + `checks: 0` is a
  distinct state from pending.
- A closing keyword in a commit trail does not distinguish "fixed this" from "mentioned this". #203
  was auto-closed by the #190/#184 merge whose own body said "Not fixed here". Re-check after every
  merge.
- Do NOT kill dotnet/testhost processes machine-wide when other agents are running — it kills their
  in-flight runs. Scope it to your own worktree.

DOCTRINE (carried forward, plus new)

From earlier saves, still true:
- A guardrail fix for an IN-FLIGHT plan must be scoped to tasks that have NOT yet succeeded.
- Never pattern-match a tool's console text you have not observed.
- Reverting for hash-drift must preserve bytes: with core.autocrlf=true, `git checkout` rewrites
  LF→CRLF. Extract the blob, write it binary, prove the method on a control file first.
- A task-level `tests-pass` keyed on a plan-wide trait is "over-broad" wearing a filter.
- CALIBRATE A LINT AGAINST REAL DOCUMENTS before shipping it.
- A TEST MUST NOT ASSUME A FACT ABOUT THE HOST.
- VERIFY THE SURFACE BEFORE ASSERTING AGAINST IT.
- PREFER THE EVENT THE PRODUCT ALREADY EMITS over a page reload.
- AN ISSUE'S PREMISE CAN BE WRONG. Five times across sessions an issue's first item already shipped.

New this session — the dominant one first:

- AN ASSERTION BOUND TO A PROXY PASSES ITS OWN EXAMINATION. This was most of the session:
  the drift test bound field NAMES while `answered` changed MEANING · the overlap gate was satisfied
  by ABSENCE (a detached element's overlap area is zero, so "the badge does not swallow the header"
  passed over a badge nobody could see) · the SDK matched CLASS NAMES an author can forge · `answered`
  counted array LENGTH not CONTENT · `SeedNotesAsync` waited for ONE increment when TWO notes were
  seeded · my own CI watch bound "a run on this branch" and reported a STALE COMMIT'S verdict as
  current · and #207's first mutation went red FOR THE WRONG REASON (a cascade from ninety lines
  earlier meant the line under test was never reached) — the agent noticed and threw it away.
- A WAIT BUG SHOWS A VALUE STILL CATCHING UP; A CORRUPTION SHOWS A VALUE THAT WENT BACKWARDS. #209
  was diagnosed as a flake, then confidently retitled into the wait family with a neat table it fit.
  It was a PRODUCT bug: `hydrate()` overwrote `state.annotations` with a snapshot taken between two
  saves, erasing the second note permanently since nothing re-renders. Pattern-matching a new symptom
  onto a real pattern is how a product bug spends two rounds being called a flake.
- FAIL-THEN-PASS ON THE SAME COMMIT IS A DEFECT TO FILE, NEVER A PASS TO BANK. Saved as a memory.
- THE CHEAP FIX AND THE CORRECT FIX OFTEN LOOK IDENTICAL FROM OUTSIDE. #208's `TryGetValue` probe
  would have worked for nested blocks and MASKED a genuine divergence on top-level ones, converting
  a loud crash into a silent wrong anchor. Only the structural guard is right, and the symptom does
  not distinguish them.
- THE ONLY TRUSTWORTHY CONTROL FOR "WHAT THE SDK DID TO A DOCUMENT" IS THE EXPORTED ARTIFACT. The
  suggested layout gate compared annotated-page vs unannotated-page — and the bug deleted the
  author's element from BOTH, so it would have agreed perfectly while both sides were wrong.
- REPRODUCE, DO NOT REASON. The flatten tiers were READ not inferred; the answer-destruction was
  WITNESSED; #207's render showed a worse defect than predicted; #202 checked that free-text is a
  `<textarea>` rather than assuming `<input>`, which would have flipped its whole conclusion.
- FILTERING AT THE POINT OF CAPTURE DESTROYS THE EVIDENCE YOU ARE FILTERING FOR. A failing test's
  name was lost to a `grep` in the pipeline. Redirect to a file, then read the file.
- NEVER `dotnet test --no-build` AFTER A FAILED BUILD — it runs stale assemblies and reports green.
  And never infer a run's state by counting processes; read the log. An orphaned testhost holds file
  locks and produces MSB3027.
- A VERIFIER MUST SAY WHAT IT CANNOT PROVE. `charter verify` prints its no-independent-witness
  disclaimer ON SUCCESS, because a green verify WILL otherwise be quoted in a post-mortem as proof a
  run was proper. Its milestone zero was the NEGATIVE suite — the inputs it exits 0 on that a reader
  would expect it to catch — because that decides whether the verb should exist at all.
- A DIAGNOSTIC THAT CRIES TAMPERING AT A LINE-ENDING REWRITE GETS IGNORED WITHIN A WEEK. A hash
  mismatch always FAILS, but the recompute labels the cause and bounds the claim.
