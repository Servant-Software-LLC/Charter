REPO: C:\DevAI\Charter
TOPIC: (none)

CONTEXT:
Building Charter wave-by-wave via the Guardrails pipeline. Charter is the front door to
an agentic delivery pipeline (Charter -> Guardrails -> firstmate): an AI authors a
block-structured markdown plan; a human reviews it in-browser and comments in place; the
reviewed markdown feeds Guardrails, which breaks it into a verified task DAG. C#/.NET,
single self-contained native binary. Combines Lavish's comment-in-place review loop with
Builder.io visual-plan's block authoring. Plan of record:
docs/plans/01-combine-lavish-and-visual-plan.md (6 milestones M1-M6).
Suite metaphor: "Charter the trip -> Guardrails keep you between the lines ->
firstmate/gnhf do the driving."

WAVE STATUS (as of 2026-07-19):
- Wave 1 (renderer-core): DONE, merged to master.
- Wave 2 (review-server): DONE, merged (ce143a2), 24 tests.
- Wave 3 (annotation-feedback): DONE, merged (edb5644), 33 tests.
- Wave 4 (rich-blocks): DONE, merged (44fc8b8), 73 tests green (Core 48 + Server 25).
- Wave 5 (export-handoff, M5): current JIT stub — the run honest-halts here. NEXT to break down.
- Wave 6 (agent skill + polish, M6): not yet stubbed (auto-created one-ahead when wave-5 is authored).
Single run resumed across all waves, runId 2026-07-17T12-43-37Z-35db (segments: wave-2 0a048e57,
wave-3 3ae04fb1, wave-4 86bf4d30 then cc2c17b6 after the hand-fix). Plan branch
guardrails/01-combine-lavish-and-visual-plan is at fc581af (wave-04 marker). Tool: preview.43.

WHERE THE PROJECT HISTORY LIVES (important):
- This repo (C--DevAI-Charter) started with NO prior Claude sessions. The real Charter
  design/genesis conversation lives in the lavish-axi project folder (work was done while
  cwd'd there).
- Main history session: 9f0023aa-e59d-4202-b292-acc3ef6786ce (7.2 MB, cwd
  C:\DevAI\Samples\lavish-axi). Resume: claude --resume 9f0023aa-... (from lavish-axi).
- Charter-related memory saved under lavish-axi (NOT here):
  C:\Users\David\.claude\projects\C--DevAI-Samples-lavish-axi\memory\charter-guardrails-firstmate-autonomous.md
- Charter repo memory store (C--DevAI-Charter/memory/) has 4 memories — see MEMORY.md:
  deferral-files-github-issue, charter-zero-telemetry, autonomy-dial-model,
  guardrails-skills-install.

COMPLETED (cumulative across sessions):
- WAVE-1: landed the renderer core from a stranded temp worktree onto master.
- WAVE-2: JIT draft (f41f1d5), /guardrails-review (F1 hardened — the e2e path-traversal check
  was a no-op because HttpClient/http.sys strip "../"; made it a raw-socket defense-in-depth
  check, unit test is the authoritative confinement proof), mark-reviewed (dac0182), run,
  merged (ce143a2). Review server: loopback + capability key + path-confinement + SDK injection.
- Mined the genesis session (9f0023aa) for the DESIGN & HISTORY section below.
- WAVE-3: broke down via charter-architect (6 tasks, ae4b32b); review NO blockers; mark-reviewed
  (cac6834); ran; merged (edb5644). Shipped: AnnotationStore (single-writer/locked + race test),
  the annotation HTTP API (/api/sessions, POST /api/{key}/prompts, /api/poll long-poll, /events
  SSE; anchor->source-line via SourceMap.LineForAnchor; same-origin CSRF), the embedded JS SDK
  sdk/charter-annotate.js (CharterAnnotate; element/text-range/diagram-node; postMessage; MIT/Lavish),
  and SDK wiring (embed + replace the wave-2 placeholder).
- Filed Guardrails #365 (JIT next-wave-stub) -> FIXED in preview.43 (PR #367). Installed preview.43
  + synced skills via `guardrails skills install --force` (CLI update alone leaves ~/.claude/skills
  stale — wrote the guardrails-skills-install memory). Filed Charter #8 (browser-half deferral).
- WAVE-4 (the big one, 15 tasks): broke down via charter-architect (4c1c849) — BlockKind
  {Diagram,Comparison,Question,Diff}, the 4 block renderers (serialized 04->06->08->12 on the shared
  CharterMarkdown/Renderer/SourceMap; test-authors parallel), offline-vendored Mermaid, the
  :::question schema (QuestionSpec C# record + known-bad reject) + form, a NEW /api/{key}/answers +
  AnswerStore + AnswerApi, and the SDK submit surface. Sub-anchors per comparison-row/diff-line
  (SourceMap extended).
  * MERMAID needsHuman + fix: pre-vendored mermaid@11.16.0 to master (88f8514) + reframed task 02 to
    confirm-present. But the run's task worktrees branch from the run BASE (690de91 on the plan
    branch), and master@88f8514 is on a DIVERGENT lineage -> the file was absent -> task 02 halted
    needs-human. Resolved via the guardrails #197 hand-fix: committed mermaid.min.js +
    MERMAID-VERSION.txt onto the PLAN BRANCH _integration worktree with a PLAIN, TRAILER-LESS message
    (58f51af — no Guardrails-* trailer, so the refuse floor protects it). Resume picked it up.
  * /guardrails-review (via charter-devils-advocate) found 1 BLOCKER + 4 WEAK + 3 NIT. BLOCKER: a
    diagram renderer emitting mermaid.initialize()/run() + a DEAD MermaidResource ref but never
    INLINING the library bytes passed every guardrail (a :::diagram that won't render offline). Fixed:
    task 03 asserts the inlined marker __esbuild_esm_mermaid_nm reaches the rendered HTML; covers
    enforces it. WEAK fixes: quote-blind CDN scan, bare-`answers` SDK teeth, satisfiable known-bad
    token, unpinned answer round-trip. All 7 fixed + #302 two-sided verified (3e8cc9f); mark-reviewed.
  * Ran, completed all 15 tasks (exit passed), merged to master (44fc8b8 — resolved a trivial
    .gitattributes conflict from both branches adding the mermaid -text rule; mermaid byte-exact).
    73 tests green; DiagramBlock tests confirm the library IS inlined. Pushed.
- Filed Guardrails #370 (research/resource-acquisition tasks that would needsHuman — detect +
  pre-resolve at breakdown; PLUS the refinement: place the pre-vendored resource on the run's
  committed BASE, not master) and #373 (run-side gap: no channel to inject a resource into an
  in-flight run; worktrees branch from the run base; overwatcher #269 auto-correct case).
- Several context saves this session (071203, 072225, 162956, this one).

WAVE-4 DECISIONS (settled — draft matched, no folder edits needed):
- Browser E2E (Mermaid render/pan-zoom/node-annotation, question-form submit) DEFERRED — v1 guards
  renderer/schema/server-round-trip deterministically. Charter #8 EXTENDED to cover it.
- `:::question` answer submission: dedicated POST /api/{key}/answers + GET /api/answers + AnswerStore
  (NOT reusing /prompts+/poll — avoids breaking a wave-3 golden). target human/agent stored+echoed.
- Mermaid: vendored offline (src/Charter.Core/assets/mermaid.min.js, v11.16.0 UMD, exposes
  globalThis.mermaid) — NEVER a CDN <script src> (portable-artifact invariant).
- Sub-block anchors: content-derived StableId(<row/line text>); SourceMap resolves per-row/line.

NEXT STEPS:
- WAVE-5 JIT breakdown (M5: export + Guardrails handoff — minimal self-contained HTML export +
  emit canonical reviewed markdown; the WAVE-1 HANDOFF FIXTURE is the acceptance gate). Re-invoke
  /plan-breakdown in JIT mode on the wave-05-export-handoff stub against materialized wave-4 code.
  The synced preview.43 skill auto-creates the wave-6 stub (#365 fixed). PER DAVID'S RULE: research
  any resource/research task (like Mermaid) UP FRONT so it doesn't needsHuman mid-run — AND place any
  pre-vendored resource on the run's plan branch, not master (the wave-4 lesson; #370/#373).
- After wave 5: wave 6 (M6 agent skill + polish). Charter #7 (firstmate autonomous/headless).
- Optional cleanup: prune old %TEMP%\guardrails-worktrees\... segments once satisfied all merged.

KEY FILES (relative to repo root):
- docs/plans/01-combine-lavish-and-visual-plan.md  (plan of record)
- docs/plans/01-combine-lavish-and-visual-plan/{diagram.md,diagram.html,guardrails.json,guardrails.baseline}
- docs/plans/01-combine-lavish-and-visual-plan/wave-0{1..4}-*/**  (authored+run); wave-05-export-handoff/ (JIT stub)
- docs/plans/01-combine-lavish-and-visual-plan/state/guardrails-review.json (tracked; run.json/state.json/logs gitignored)
- src/Charter.Core/{BlockModel,CharterRenderer,SourceMap,MermaidResource,QuestionSpec}.cs ; assets/{mermaid.min.js,MERMAID-VERSION.txt}
- src/Charter.Cli/Program.cs (render + review verbs)
- src/Charter.Server/{ReviewServer,IReviewServer,ReviewSession,ReviewServerOptions,CapabilityKey,
  PathConfinement,SdkInjector,Annotation,AnnotationStore,AnnotationApi,SdkResource,Answer,AnswerStore,AnswerApi}.cs
- sdk/charter-annotate.js  (embedded JS SDK — injected at serve time only; wave-4 added the answer-submit surface)
- tests/Charter.Core.Tests/** (incl. Diagram/Comparison/Diff BlockTests, QuestionSchema/Form) ;
  tests/Charter.Server.Tests/** (incl. AnswerApiTests)
- .gitattributes (mermaid.min.js -text byte-exact rule) ; global.json (pins .NET 8.x — no .slnx)

CROSS-REPO:
- lavish-axi (C:\DevAI\Samples\lavish-axi): genuine Charter genesis conversation (session 9f0023aa)
  + the Charter firstmate-autonomous memory; the reference impl Charter re-derives from.
- Guardrails: Charter's downstream consumer; local checkout at C:\DevAI\Guardrails. Tool preview.43.
  Issues touched this session: #365 (JIT stub — FIXED preview.43), #370 (resource/research task
  detection + run-base placement), #373 (in-flight resource injection / worktree-lineage gap),
  #361 (autonomy-dial / Charter #7), #269 (overwatcher — the #373 auto-correct home). Also #360/#354/#176/#353.
- Tricentis (David's employer): MacBook ~end of month (2026-07) after a Windows career; wants to
  introduce the MDX-planning idea there. WHY .NET-on-Mac install friction / signing matter.

BLOCKERS/NOTES:
- RUN-LINEAGE DIVERGENCE (the wave-4 lesson): I merge each completed wave's plan branch to master,
  but the run CONTINUES on its own plan branch (guardrails/01-combine-lavish-and-visual-plan). So a
  resource added to MASTER mid-run does NOT reach the run's task worktrees (they branch from the run
  base). Plan-folder edits (prompts/guardrails) ARE read live from the checkout; CODE artifacts must
  be on the run base. Fix a mid-run resource via the #197 hand-fix: commit it to the plan branch's
  _integration worktree (C:\Users\David\AppData\Local\Temp\guardrails-worktrees\...\_integration)
  with a PLAIN TRAILER-LESS message (never a Guardrails-* trailer, #322). Tracked as #373.
- A guardrails.exe running `docs/plans/autonomous-mode-impl` is an UNRELATED run (another session),
  NOT the Charter run — don't confuse it for ours when checking process state.
- Charter GH issues (Servant-Software-LLC/Charter): 8 open. #1-#6 deferrals/decisions; #7 firstmate
  autonomous; #8 browser-E2E deferral (extended for wave-4).
- After ANY guardrails CLI update: run `guardrails skills install --force` (see the memory).
- User env: never `cd <path> && git ...`; always `git -C <root> ...` (PreToolUse hook blocks it).
- Untracked per-wave diagram view files (wave-*/diagram.{html,md}) sit in the tree — regenerable
  `guardrails graph <wave>` outputs, left untracked.

=====================================================================
DESIGN & HISTORY (distilled from genesis session 9f0023aa — deltas NOT in the plan doc)
=====================================================================

STANDING USER PREFERENCES / DIRECTIVES (David):
- EVERY deferral gets a GitHub issue ("if we just state it in the plan, it will be
  forgotten after the plan is executed"). Distinction he drew: DEFERRAL ("maybe later")
  -> issue; DECISION ("we chose not to", e.g. telemetry-off) -> NO issue.
- Zero telemetry is a hard preference ("Yes, zero telemetry is my preference").
- The autonomy DIAL is David's idea: auto-proceed vs halt-and-escalate is an
  adjustable dial; AI assesses criticality and the dial arbitrates. But HARD BLOCKERS
  override the dial ("even if the dial is set to not stop at all costs, it will have
  to stop").
- Discoverability meta-rule: a shipped capability that requires grepping a 2,100-line
  skill to discover is itself a defect; a skill must surface its capabilities "at the
  altitude a consumer first looks" (-> Guardrails #354).
- Wave labels: never strip the wave-NN-slug prefix as a standalone change — that hides
  waves further; only remove it coupled to the subgraph work that replaces it.
- Research/resource tasks: research anything that would cause a mid-run needsHuman UP FRONT
  during /plan-breakdown (Guardrails #370), rather than discovering it at run time.

KEY DECISIONS & RATIONALE (roads taken AND rejected):
- Charter did NOT start as markdown+directives. First sketch was "go totally MDX,
  keep my own OSS" (full MDX + Preact/React SSR + block components). markdown+`:::`
  directives (Markdig) won AFTER a format-research pass: gets ~90% of MDX's benefit
  without dragging a JS compiler+framework into a codebase with neither. The real
  driver is LEANNESS, not just "MDX can't run in C#".
- Load-bearing insight that de-risked the whole thing: commenting depends on the
  loopback SERVER, not the authoring FORMAT. "server vs static" and "HTML vs MDX vs
  markdown" are INDEPENDENT axes; annotations anchor to DOM, which doesn't care whether
  a human, agent, or compiler produced the HTML. So: render markdown -> HTML at BUILD
  time -> serve through the same server. Client-side/in-browser render was explicitly
  rejected (makes the HTML inert without a bundle; the exact property Lavish avoids).
- No standalone M0 spike: David argued the DAG makes a slice's orientation value
  redundant; assistant sharpened it ("a DAG is a specification, not evidence") and
  folded M0's risk-proofs (anchor-survival, handoff-validates) into wave-1 acceptance
  guardrails.
- Distribution = C# native binary + Homebrew (Guardrails #353), chosen over npx-TS and
  over a Go/Rust rewrite. Charter is greenfield and COULD have gone npx (like
  lavish/gnhf/firstmate) but deliberately matched the C# suite for consistency.
- Charter's CI/CD is a PORT of Guardrails' pipeline (already-debugged ci.3), which is
  why Charter's first release dry-run went green immediately.
- Charter.Core is intentionally ONE implementation task (not split model/renderer/
  source-map) to avoid Guardrails' #176 transitive-compile diamond (test project
  compiles as a unit, references all three types). A documented WEAK sizing, on purpose.

NAMING ("Charter"):
- Metaphor: the founding mandate you agree BEFORE the work; commissioning a voyage
  (nautical, meshes with firstmate); royal charters were lavishly illuminated docs (nod
  to Lavish); transport-agnostic ("charter a coach as readily as a boat") so it also
  rides Guardrails' road metaphor. Verbs cleanly ("charter the work").
- Etymology clincher: charter/chart/cartography all descend from Latin "charta"
  (paper/map) -> the map/visual DNA is already inside the name.
- Tool and its output share the name ("you produce a Charter with Charter").
- Availability: ServantSoftware.Charter free on NuGet; Homebrew free; npm taken
  (irrelevant for C#/NuGet). Name is diluted in company-land (Charter Communications
  etc.) — "you won't own the search results". (charter.dev domain is NOT an action
  item — OSS project, no purchase planned unless it becomes very popular.)
- Rejected names (with reasons): Gloss (dead Swift lib), Vellum (Mac app), Slate ("the
  surface, not the plan"), Planable (AVOID - established SaaS in the adjacent
  collab/commenting space), Onramp (David's #2 but OnRamp.us is a VC-backed AI-agent
  platform - riskiest), FlightPlan, Marginalia, Rubric, Folio, Illume, Redline, Easel,
  and the whole map family (Atlas/Cartograph/Contour/Legend/Charta - all taken).
- SEXTANT is reserved for a FUTURE telemetry/verification tool (instruments name a
  "know where you are / stay on course" function = Guardrails' job). "bearings" is
  already a firstmate skill.

CONTRACTS / FORMAT DETAILS (settled in conversation, not all in the plan):
- Lavish server contract Charter models on: loopback 127.0.0.1:4387; POST
  /api/:key/prompts -> SessionStore -> feedback event -> `poll` long-poll -> agent.
  SDK injection is literally html.replace(/<\/body\s*>/i, ...) — a regex string
  replace, NO DOM library anywhere in Lavish. (Charter wave-3 implemented this contract:
  /api/{key}/prompts + /api/poll long-poll + /events SSE + a single-writer AnnotationStore;
  wave-4 added /api/{key}/answers for :::question submissions.)
- visual-plan block catalog (reference superset): diagram, data-model, api-endpoint,
  openapi-spec, json-explorer, diff, file-tree, annotated-code, checklist, question-form,
  wireframe, custom-html. Charter's catalog is a deliberate subset (wave-4 added
  diagram/comparison/question/diff to the wave-1 prose/heading/list/table/code/note/warn).
- CORRECTION (David): visual-plan DOES have interactive elicitation (question-form +
  visual-intake + create-visual-questions). Charter's :::question REPRODUCES
  question-form; the input gap it fills is in base CommonMark, NOT in visual-plan.
- Charter release mechanics: published binary is Charter.Cli.exe RENAMED to charter.exe
  via APPHOST rename at publish — NOT via -p:AssemblyName (that renames Core too ->
  NETSDK1152 collision). NuGet gated behind PUBLISH_NUGET=true repo var + Trusted
  Publishing (off by default; dry-runs never touch NuGet). Version 0.1.0.

FIRSTMATE / GUARDRAILS / AUTONOMY:
- firstmate = agent-distribution system dispatching autonomous crewmates in isolated
  worktrees, escalating only real decisions; a tool it drives can't block on an
  interactive human loop. Two-tier skills: public skills/ (self-contained) vs private
  .agents/skills/ (metadata.internal:true). firstmate is Shell; gnhf is TypeScript.
- Autonomy dial three-layer model (Guardrails #361): (1) judgment call -> dial
  arbitrates halt-vs-best-guess by criticality/confidence; (2) transient hard blocker
  -> bounded wait + backoff with configurable ceiling; (3) permanent blocker or retries
  exhausted -> unconditional halt-and-escalate with full context. "The dial governs
  judgment, not physics." The overwatcher (#269) is the run-side agent that would apply
  the auto end of this dial — e.g. auto-resolving the mechanical mermaid resource gap (#373).
- Charter #7 is deliberately THIN: its one decision is render-and-persist vs serve, no
  mid-run gates -> no dial. Autonomous Charter = headless render + persist deliverable
  to disk + exit, WITHOUT the loopback comment server / long-poll. The "why": firstmate
  has no human present, but we still want deliverables persisted for forensic hindsight.
- Durable intent beyond v1: a cross-orchestrator MDX-planning capability usable from
  gnhf + firstmate public tiers AND at Tricentis.

SECURITY & TRUST (re-derived from Lavish's source, not inherited):
- Store-concurrency risk origin: Lavish does whole-file read-modify-write of session
  JSON, so concurrent poll + prompts can race -> Charter's open item: single-writer/lock.
  (Wave-3 AnnotationStore implements this single-writer/locked, proven by a race test.)
- Export MUST strip the annotation SDK (a static page can't route comments home). Lavish
  reverted PR #16 (asset injection) to protect "the HTML is the artifact" portability
  (Lavish issue #37). Charter's markdown/block front-end must be ADDITIVE; portable HTML
  stays canonical. (SDK injected only at serve time; render output stays SDK-free — enforced
  structurally because SDK injection lives in Charter.Server, not Charter.Core. NOTE: wave-4
  Mermaid is different — the diagram RUNTIME is inlined into the rendered artifact by
  Charter.Core so a saved :::diagram renders offline; that's a RENDER concern, distinct from
  the serve-time annotation SDK.) This export/SDK-strip discipline is wave-5's (M5) core work.
- CRLF->LF gotcha: a CRLF shebang breaks install.sh on macOS/Linux ("bad interpreter");
  pinned LF in .gitattributes (Charter's install.sh is a port -> inherits this). Wave-4 added
  a `-text` rule so the vendored mermaid.min.js stays byte-exact.

CI/CD & TOOLING GOTCHAS (from porting Guardrails' pipeline):
- .slnx incompatibility: .NET 10 SDK emits .slnx that the 8.x SDK/CI can't read. Charter
  uses a classic .sln pinned to .NET 8.x via global.json. Don't regenerate the solution
  with a newer SDK.
- Release tag uses `workflow_run` trigger, NOT `release: published` (a GITHUB_TOKEN-
  created release does not fire event chains). `-ci.` tag guard skips NuGet publish so
  throwaway v0.0.0-ci.N dry-run tags don't pollute NuGet.
- TAP_PAT secret is per-repo; must be set org-wide (Servant-Software-LLC / Guardrails,
  Charter) or the Homebrew tap-bump fails.
- Sandbox static-analysis guard blocks `Remove-Item -Recurse -Force` when authoring
  guardrail scripts in this harness.
- macOS: a CLI binary can't be stapled (stapler only does .app/.dmg/.pkg); notarization
  is server-side, Gatekeeper checks online on first run; JIT/unsigned-exec entitlements
  may need tuning. Relevant when Charter's signed release happens.
- After a guardrails CLI (dotnet tool) update, the bundled Claude skills in
  ~/.claude/skills/ are NOT auto-refreshed — run `guardrails skills install --force`.
- GUARDRAILS RUN ISOLATION (wave-4 lesson): the run's task worktrees branch from the run's
  own base on the plan branch, NOT from master. A resource added to master mid-run does not
  propagate to the run. Run allowedTools have no network tool, so a task can't fetch anything.
  Supply a mid-run resource via the #197 hand-fix (trailer-less commit on the plan branch
  _integration worktree). Tracked as Guardrails #370 (breakdown-side) + #373 (run-side).

PRIOR ART (opinions worth keeping):
- Lavish's entire layout-audit subsystem (overflow/clip/overlap + gate) exists ONLY
  because agents hand-write HTML — a cost Charter's vetted block components remove "by
  construction". Direct motivation for the block approach.
- visual-plan is MORE of a live webhost than Lavish, not less: its default deliverable
  is always a hosted Agent-Native Plan via plan.agent-native.com MCP; even local mode
  renders through the hosted UI. Corrects the intuition that visual-plan is the
  lighter/local option.
- Lavish tagline "HTML is the new markdown" -> pitching a markdown front-end as
  "markdown replaces HTML" collides head-on; hence Charter is framed independent/additive.
- Lavish telemetry = ~194 LOC best-effort HTTP POST to a hardcoded Umami endpoint,
  default-ON. The sins to avoid are vendor-SDK + hardcoded endpoint + default-on — a
  configurable HttpClient POST (or OTLP) has none. Sharpens Charter's telemetry rule (#6).
- Builder.io pipeline internals: MDX -> typed components -> normalized to JSON,
  validated with Zod -> renderer. Load-bearing part is the strict SCHEMA
  ("type-safety for natural language"), reproduced by markdown+directives-validated-to-
  C#-records minus the JS runtime. (Charter wave-4 QuestionSpec is exactly this — a C#
  record + known-bad-reject validation for the :::question block.)
- visual-recap premise ("the same plan data model serves both directions") is exactly
  Charter's direction-agnostic architecture -> keep plan-only assumptions out of the
  block model + source-map so recap-mode (#1) stays cheap.
