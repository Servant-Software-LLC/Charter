# Auto-generated by the bump-tap workflow — do not edit by hand.
# Source template: .github/templates/charter.rb.tmpl in Servant-Software-LLC/Charter.
# Placeholders (@VERSION@, @SHA_*@) are substituted per release from the release checksums.
class Charter < Formula
  desc "Visual, reviewable plans your agent drafts and you annotate in place"
  homepage "https://github.com/Servant-Software-LLC/Charter"
  version "@VERSION@"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/Servant-Software-LLC/Charter/releases/download/v#{version}/charter-#{version}-osx-arm64.tar.gz"
      sha256 "@SHA_OSX_ARM64@"
    end
    on_intel do
      url "https://github.com/Servant-Software-LLC/Charter/releases/download/v#{version}/charter-#{version}-osx-x64.tar.gz"
      sha256 "@SHA_OSX_X64@"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/Servant-Software-LLC/Charter/releases/download/v#{version}/charter-#{version}-linux-arm64.tar.gz"
      sha256 "@SHA_LINUX_ARM64@"
    end
    on_intel do
      url "https://github.com/Servant-Software-LLC/Charter/releases/download/v#{version}/charter-#{version}-linux-x64.tar.gz"
      sha256 "@SHA_LINUX_X64@"
    end
  end

  # Single self-contained binary — no sibling assets yet, so install it straight into bin.
  def install
    bin.install "charter"
  end

  test do
    assert_match "charter", shell_output("#{bin}/charter --version")
  end
end
