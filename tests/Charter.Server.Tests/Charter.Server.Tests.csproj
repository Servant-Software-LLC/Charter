<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
    <!-- GitTrackingTests reads a real `git init`ed temp repo: git is a supported ABSENCE (Charter must work on
         a machine that has never seen it), so those cases SKIP rather than fail. Same version the browser
         acceptance suite already uses. -->
    <PackageReference Include="Xunit.SkippableFact" Version="1.5.23" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Charter.Server\Charter.Server.csproj" />
  </ItemGroup>

</Project>
